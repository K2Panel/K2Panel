# ==============================================================================
# Docker Compose Configuration - Shared Services
# ==============================================================================
# Shared infrastructure for Blue-Green Deployment
# PostgreSQL and Redis are shared between Blue and Green environments
#
# Usage:
#   docker-compose -f docker-compose.shared.yml up -d
# ==============================================================================

version: '3.8'

# ==============================================================================
# Shared Services
# ==============================================================================
services:
  
  # ============================================================================
  # Shared PostgreSQL Database Service
  # ============================================================================
  postgres-shared:
    image: postgres:15-alpine
    container_name: k2panel_postgres_shared
    
    # Environment file
    env_file:
      - .env
    
    # Volumes
    volumes:
      # Persistent PostgreSQL data
      - postgres_data_shared:/var/lib/postgresql/data
    
    # Network
    networks:
      - k2panel_network
    
    # Restart policy
    restart: always
    
    # Health check
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER:-k2panel_user} -d $${POSTGRES_DB:-production_db}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
  
  # ============================================================================
  # Shared Redis Caching Service
  # ============================================================================
  redis-shared:
    image: redis:7-alpine
    container_name: k2panel_redis_shared
    
    # Additional commands
    command: >
      redis-server
      --appendonly yes
      --appendfsync everysec
      --maxmemory 256mb
      --maxmemory-policy allkeys-lru
    
    # Volumes
    volumes:
      # Persistent Redis data
      - redis_data_shared:/data
    
    # Network
    networks:
      - k2panel_network
    
    # Restart policy
    restart: always
    
    # Health check
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 10s
  
  # ============================================================================
  # Shared Prometheus - Metrics Collection Service
  # ============================================================================
  prometheus-shared:
    image: prom/prometheus:v2.48.0
    container_name: k2panel_prometheus_shared
    
    # Commands
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--storage.tsdb.retention.time=15d'
      - '--web.console.libraries=/usr/share/prometheus/console_libraries'
      - '--web.console.templates=/usr/share/prometheus/consoles'
      - '--web.enable-lifecycle'
    
    # Ports (قم بإزالة هذا في الإنتاج واستخدم nginx reverse proxy)
    ports:
      - "9090:9090"
    
    # Volumes
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
      # Alert rules - قواعد التنبيه
      - ./prometheus-rules.yml:/etc/prometheus/rules/prometheus-rules.yml:ro
      - prometheus_data_shared:/prometheus
    
    # Dependencies
    depends_on:
      - alertmanager-shared
    
    # Network (with alias for datasource compatibility)
    networks:
      k2panel_network:
        aliases:
          - prometheus  # Allow Grafana to connect via http://prometheus:9090
    
    # Restart policy
    restart: always
    
    # Health check
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:9090/-/healthy"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
  
  # ============================================================================
  # Shared Grafana - Visualization & Dashboards Service
  # ============================================================================
  grafana-shared:
    image: grafana/grafana:10.2.2
    container_name: k2panel_grafana_shared
    
    # Environment
    environment:
      # ⚠️ يجب تعيين هذه المتغيرات في .env! لا توجد قيم افتراضية
      - GF_SECURITY_ADMIN_USER=${GRAFANA_ADMIN_USER}
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_ADMIN_PASSWORD}
      - GF_SERVER_ROOT_URL=%(protocol)s://%(domain)s:%(http_port)s/
      - GF_SERVER_DOMAIN=${GRAFANA_DOMAIN:-localhost}
      - GF_DATABASE_TYPE=sqlite3
      - GF_AUTH_ANONYMOUS_ENABLED=false
      - GF_LOG_MODE=console
      - GF_LOG_LEVEL=info
    
    # Ports (قم بإزالة هذا في الإنتاج واستخدم nginx reverse proxy)
    ports:
      - "3000:3000"
    
    # Volumes
    volumes:
      - grafana_data_shared:/var/lib/grafana
      - ./grafana-dashboard-k2panel.json:/var/lib/grafana/dashboards/k2panel.json:ro
      # Datasources provisioning - Prometheus & Loki
      - ./grafana-datasource.yml:/etc/grafana/provisioning/datasources/prometheus.yml:ro
      - ./grafana-loki-datasource.yml:/etc/grafana/provisioning/datasources/loki.yml:ro
      - ./grafana-dashboard-provisioning.yml:/etc/grafana/provisioning/dashboards/dashboard.yml:ro
    
    # Dependencies
    depends_on:
      prometheus-shared:
        condition: service_healthy
    
    # Network
    networks:
      - k2panel_network
    
    # Restart policy
    restart: always
    
    # Health check
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
  
  # ============================================================================
  # Shared Alertmanager - Alert Management Service
  # ============================================================================
  alertmanager-shared:
    image: prom/alertmanager:latest
    container_name: k2panel_alertmanager_shared
    
    # Commands
    command:
      - '--config.file=/etc/alertmanager/alertmanager.yml'
      - '--config.expand-env=true'
      - '--storage.path=/alertmanager'
      - '--web.external-url=http://localhost:9093'
    
    # Ports (قم بإزالة هذا في الإنتاج واستخدم nginx reverse proxy)
    ports:
      - "9093:9093"
    
    # Volumes
    volumes:
      - ./alertmanager.yml:/etc/alertmanager/alertmanager.yml:ro
      - alertmanager_data_shared:/alertmanager
    
    # Environment file
    env_file:
      - .env
    
    # Network (with alias for datasource compatibility)
    networks:
      k2panel_network:
        aliases:
          - alertmanager  # Allow Prometheus to connect via alertmanager:9093
    
    # Restart policy
    restart: always
    
    # Health check
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:9093/-/healthy"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
  
  # ============================================================================
  # Shared Loki - Centralized Logging Service
  # ============================================================================
  loki-shared:
    image: grafana/loki:2.9.0
    container_name: k2panel_loki_shared
    
    # Loki is internal-only for security. Access via Grafana.
    # No external port binding - accessible only within Docker network
    
    # Volumes
    volumes:
      - ./loki-config.yml:/etc/loki/local-config.yaml:ro
      - loki_data_shared:/tmp/loki
    
    # Commands
    command: -config.file=/etc/loki/local-config.yaml
    
    # Network (with alias for datasource compatibility)
    networks:
      k2panel_network:
        aliases:
          - loki  # Allow Grafana and Promtail to connect via http://loki:3100
    
    # Restart policy
    restart: unless-stopped
    
    # Health check
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:3100/ready"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
  
  # ============================================================================
  # Shared Promtail - Log Collection Service
  # ============================================================================
  promtail-shared:
    image: grafana/promtail:2.9.0
    container_name: k2panel_promtail_shared
    
    # Volumes
    volumes:
      - ./promtail-config.yml:/etc/promtail/config.yml:ro
      # Docker container logs
      - /var/lib/docker/containers:/var/lib/docker/containers:ro
      # Docker socket for container metadata enrichment (read-only)
      - /var/run/docker.sock:/var/run/docker.sock:ro
      # System logs
      - /var/log:/var/log:ro
      # Positions file
      - promtail_positions_shared:/tmp
    
    # Commands
    command: -config.file=/etc/promtail/config.yml
    
    # Dependencies
    depends_on:
      loki-shared:
        condition: service_healthy
    
    # Network
    networks:
      - k2panel_network
    
    # Restart policy
    restart: unless-stopped

# ==============================================================================
# Persistent Volumes
# ==============================================================================
volumes:
  # Shared PostgreSQL data
  postgres_data_shared:
    driver: local
    name: k2panel_postgres_data_shared
  
  # Shared Redis data
  redis_data_shared:
    driver: local
    name: k2panel_redis_data_shared
  
  # Shared Prometheus data
  prometheus_data_shared:
    driver: local
    name: k2panel_prometheus_data_shared
  
  # Shared Grafana data
  grafana_data_shared:
    driver: local
    name: k2panel_grafana_data_shared
  
  # Shared Alertmanager data
  alertmanager_data_shared:
    driver: local
    name: k2panel_alertmanager_data_shared
  
  # Shared Loki data
  loki_data_shared:
    driver: local
    name: k2panel_loki_data_shared
  
  # Shared Promtail positions
  promtail_positions_shared:
    driver: local
    name: k2panel_promtail_positions_shared

# ==============================================================================
# Networks
# ==============================================================================
networks:
  k2panel_network:
    driver: bridge
    name: k2panel_network

# ==============================================================================
# Important Notes
# ==============================================================================
#
# 1. Start shared services first:
#    docker-compose -f docker-compose.shared.yml up -d
#
# 2. Then start blue or green:
#    docker-compose -f docker-compose.blue.yml up -d
#    docker-compose -f docker-compose.green.yml up -d
#
# 3. Shared services persist between deployments
#
# ==============================================================================
